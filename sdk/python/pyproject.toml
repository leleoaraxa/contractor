[project]
name = "contractor-sdk-python"
version = "0.1.0"
description = "Contractor SDK (Python) — Stage 2"
requires-python = ">=3.10"
dependencies = ["requests>=2.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
