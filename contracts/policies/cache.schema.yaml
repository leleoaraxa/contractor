type: object
required:
  - cache
properties:
  cache:
    type: object
    properties:
      enabled:
        type: boolean
      default_ttl_seconds:
        type: integer
        minimum: 0
      max_ttl_seconds:
        type: integer
        minimum: 0
    required:
      - enabled
