$schema: "https://json-schema.org/draft/2020-12/schema"
$title: "CONTRACTOR Ontology - Intents"
$type: object
$additionalProperties: false
required: [version, intents]
properties:
  version:
    type: integer
    minimum: 1
  intents:
    type: array
    minItems: 1
    items:
      type: object
      additionalProperties: false
      required: [id, title]
      properties:
        id:
          type: string
          minLength: 1
          pattern: "^[a-z][a-z0-9_]*$"
        title:
          type: string
          minLength: 1
        description:
          type: string
