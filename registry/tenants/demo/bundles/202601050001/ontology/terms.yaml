version: 1
terms:
  # Minimal routing vocabulary for demo tenant
  - term: "health"
    intents: ["health_check"]
    entities: ["platform_status"]

  - term: "status"
    intents: ["health_check"]
    entities: ["platform_status"]
